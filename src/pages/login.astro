---
  import Layout from '@layouts/layout.astro'
  import { isUser } from '@lib/supabase'

  if (isUser(Astro.cookies)) {
    return redirect('/rpg')
  }
---

<Layout title='Přihlášení'>
  <a href='/'>Andor</a>
  <h1>Login</h1>

  <form action='/api/auth/login' method='post'>
    <button value='google' name='provider' type='submit'>Přihlásit přes Google</button>
  </form>
</Layout>
